const config = {
  // The base url of the sever
  base: '',
  // The sipId, appId, appKey of yor bot
  sipId: '',
  appId: '',
  appKey: '',
  // The port for the sever to receive message
  port: 3000,
  // The version of the API you are using
  apiVersion: 'v1',
  // The chatbotId of your bot
  chatbotId: '',
  // The server's root url
  serverRoot: 'maap.5g-msg.com:30001',
}

export { config }

export { VERSION } from './version'
